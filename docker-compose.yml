version: "3"
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    volumes:
      - .:/beside_project
    env_file:
      - .env

    command: sh /beside_project/build.sh
